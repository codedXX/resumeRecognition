如果不想在服务器构建，也可以在本机导出镜像后上传：
docker save -o resume-backend.tar resume-backend:local
服务器上：
docker load -i resume-backend.tar
cd /opt/resumeRecognition/backend
docker compose up -d --no-build --pull never
docker compose ps
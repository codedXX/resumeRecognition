## 1. 岗位规则文件存储

- [x] 1.1 定义 `backend/data/roles.json` 的版本化格式、默认初始化和角色/要求校验，并用单元测试覆盖空文件、稳定 ID、重复名称、分数边界和敏感特征拒绝
- [x] 1.2 实现后端岗位文件仓储与现有岗位 CRUD API 的适配，使用进程/文件锁、临时文件和原子替换保存，并通过 API 测试验证创建、编辑、删除、排序和并发写入后 JSON 仍有效
- [x] 1.3 将运行时角色文件与源码/前端静态资源分离，补充 `.gitignore`、初始化说明和损坏文件错误处理，并验证角色文件不包含简历或评分字段

## 2. 内存化一次性分析

- [x] 2.1 实现单进程内存分析存储，迁移批次、文件状态、临时文本、岗位快照、评分和证据查询，保持现有 API 响应形状，并通过后端 API 测试覆盖上传到结果展示流程
- [x] 2.2 保证 PDF/DOCX 字节和解析文本不落盘，增加终态 TTL 清理和进程重启丢失语义测试，验证过期批次返回既有的未找到/已过期响应
- [x] 2.3 删除 SQLAlchemy/Alembic、数据库模型、session 初始化和数据库配置依赖，更新启动命令为单 worker，并通过依赖检查和启动冒烟测试确认服务不创建数据库文件

## 3. 阿里云百炼评估提供商

- [x] 3.1 复用 AgentScope OpenAI 兼容适配器接入百炼配置，支持 API Key、模型名、地域/业务空间 Base URL 环境变量，并用配置测试确认缺少配置时不会发起外部请求且密钥不会进入日志
- [x] 3.2 实现百炼结构化评分结果校验和 JSON 兼容回退，覆盖合法分数、理由、满足条件、待补证据、原文引用、越界分数和 malformed 响应，并验证单文件失败不影响同批其他文件
- [x] 3.3 增加显式环境变量门控的真实百炼冒烟测试，验证选定模型能够返回可解析 `EvaluationResult`，离线测试默认不访问外部网络
- [x] 3.4 在前端/README 中增加百炼外部处理提示和后端配置说明，验证 API Key 只存在后端环境变量且简历全文、证据全文不写入日志

## 4. 回归与交付

- [x] 4.1 更新前后端测试夹具和 API 合同，验证岗位管理、上传、评估、筛选、候选人选择和证据展示在无数据库模式下仍可用
- [x] 4.2 运行后端 pytest、前端 `npm test` 和 `npm run build`，确认测试、类型检查和生产构建全部通过
- [x] 4.3 在单 worker 本地服务中完成手工验证，检查服务重启后岗位规则保留而分析结果清空，并确认没有生成 SQLite/上传文件残留
- [x] 4.4 运行 `openspec validate "file-backed-role-config-ephemeral-bailian-evaluation" --type change --strict --no-interactive`，确认全部提案工件通过严格校验

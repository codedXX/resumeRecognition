## 1. 上传反馈模型与校验

- [x] 1.1 增加前端文件类型/大小预检和人类可读的大小格式化，验证超限或不支持文件不会静默提交并显示文件名、大小和上限
- [x] 1.2 解析上传接口返回的逐文件状态，区分全部成功、部分成功和全部失败，验证失败原因不会被通用成功提示覆盖

## 2. 提示与启动引导

- [x] 2.1 使用现有 Dialog 能力实现上传错误/警告汇总弹窗，验证标题、失败文件、原因和重新选择/关闭操作可见且具有 `alertdialog` 语义
- [x] 2.2 为“未选岗位”“无可读取文件”“批次过期”增加状态驱动的启动引导，验证按钮附近始终显示具体下一步
- [x] 2.3 为岗位选择器增加缺失状态的高亮、描述关联和可访问聚焦引导，验证键盘用户也能定位下一步
- [x] 2.4 统一成功、警告、错误提示样式和 `role=status`/`role=alert` 语义，验证网络错误和评估失败均提供可行动建议

## 3. 回归与交付验证

- [x] 3.1 增加前端交互测试，覆盖超限文件、批量部分失败、未选岗位、无可读文件、服务错误和成功等待下一步文案，并运行 `npm test`
- [x] 3.2 运行 `npm run build`，手工验证上传与评估流程在桌面和窄屏布局下的弹窗、焦点和按钮状态
- [x] 3.3 运行 `openspec validate "improve-workbench-feedback-prompts" --type change --strict --no-interactive`，确认提案工件通过严格校验

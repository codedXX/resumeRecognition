## 1. 自由文本岗位规则

- [x] 1.1 为岗位表单增加完整多行岗位规则的行为测试，覆盖段落、编号、分号、空文本校验和保存后原文保留，并先确认新断言在现有实现中失败
- [x] 1.2 调整前端岗位编辑表单，将 `evaluation_prompt` 展示为“岗位规则”长文本输入，保留现有岗位名称和合格线字段，并用组件测试验证创建/编辑时文本不被拆分或丢失
- [x] 1.3 将结构化要求编辑收纳为可选高级区域，修正保存逻辑使未修改高级区域时不删除已有要求，并用回归测试验证旧结构化岗位仍可编辑
- [x] 1.4 增加无结构化 requirements 的后端评估测试，覆盖完整岗位原文、职责/必要/优先条件区分和主观偏好不作为唯一淘汰依据，验证后端 pytest 通过
- [x] 1.5 调整评分提示和本地启发式兜底，使完整岗位原文始终参与评估、空要求列表不再产生空评分契约，并用 provider/API 测试验证结果仍包含证据和合格线判断

## 2. 自定义下拉选择器

- [x] 2.1 为岗位选择和优先级选择编写组件测试，覆盖占位符、选中项、打开/关闭、外部点击和 Escape，并先确认原生 select 实现无法满足测试
- [x] 2.2 实现受控自定义 Select 组件，提供圆角纸本橙视觉、选中/高亮态、combobox/listbox/option ARIA 语义和焦点样式，并通过组件测试验证渲染与状态同步
- [x] 2.3 为 Select 增加 ArrowUp/ArrowDown、Enter 键盘选择及 Escape 关闭行为，验证键盘测试和现有前端测试全部通过
- [x] 2.4 用自定义 Select 替换工作台岗位选择和岗位表单优先级选择，验证选择岗位、保存岗位和启动评估流程的 API 调用合同不变

## 3. 视口自适应工作台

- [x] 3.1 增加桌面空状态、矮窗口、长证据内容和移动宽度的布局测试或可重复浏览器检查，记录当前固定最小高度导致滚动条的失败基线
- [x] 3.2 调整工作台和应用外壳的视口高度计算，移除固定 680px 约束并保留 `dvh`/内容溢出回退，验证常见桌面空状态不因布局本身产生滚动条
- [x] 3.3 验证长岗位规则、长证据和弹窗内容仍可滚动，移动端面板继续自然纵向排列，确保没有全局 overflow 隐藏导致内容剪裁

## 4. 回归与交付

- [x] 4.1 运行后端 pytest，确认岗位规则、评估提示和 API 兼容测试全部通过
- [x] 4.2 运行前端 `npm test` 和 `npm run build`，确认组件测试、类型检查和生产构建全部通过
- [x] 4.3 使用本地浏览器检查桌面空状态、下拉菜单视觉/键盘操作、岗位规则长文本和移动端滚动，并记录最终结果
- [x] 4.4 运行 `openspec validate "freeform-role-rules-custom-select-viewport-fit" --type change --strict --no-interactive`，确认所有提案工件通过严格校验

## 1. 中文文案与组件结构

- [x] 1.1 在 `frontend/src/App.tsx` 和相关文案映射中替换工作台可见英文解释性文案，保留 PDF、DOCX、数字和状态符号，并通过组件测试断言空状态、成功状态和失败状态均显示中文
- [x] 1.2 更新岗位、文件队列、候选人、证据对照、合格分数和智能初筛建议等分区标题及动态状态，运行前端文案测试确认无遗漏的用户可见英文标签

## 2. 圆角与可读性样式

- [x] 2.1 在 `frontend/src/styles.css` 增加分层圆角令牌，并应用到工作台外框、主要区域、上传区、证据块、按钮、筛选器、输入框、标签和岗位管理弹窗；通过桌面截图检查圆角连续、细线分隔仍清晰
- [x] 2.2 调整 `frontend/src/styles.css` 的中文字体回退链和字号层级，使正文、辅助文字、控件、候选人/文件名、证据引用和审阅备注达到设计目标；通过桌面与移动端截图检查换行、裁剪和信息层级
- [x] 2.3 保持键盘焦点、触控命中区、响应式单列重排和 reduced-motion 规则，执行可访问性/响应式测试确认无水平溢出且交互状态仍可辨识

## 3. 回归验证与交付

- [x] 3.1 运行前端组件测试和相关 lint/typecheck，确认文案、上传、岗位选择、评估、筛选、候选人选择及证据展示回归通过
- [x] 3.2 运行前端生产构建并记录成功输出，确认新增样式和文案不会引入编译或打包错误
- [x] 3.3 在桌面和窄屏浏览器完成人工验收，检查中文覆盖、圆角表面、字号可读性、键盘焦点和 reduced-motion 行为，并保存必要的截图或验收记录
- [x] 3.4 运行 `openspec validate "polish-chinese-rounded-readable-ui" --type change --strict --no-interactive`，确认变更工件和任务格式通过严格校验
